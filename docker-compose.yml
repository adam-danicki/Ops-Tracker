services:
  db:
    image: postgres:16
    container_name: ops_tracker_db
    environment:
      POSTGRES_USER: ops
      POSTGRES_PASSWORD: ops
      POSTGRES_DB: ops_tracker
    ports:
      - "5432:5432"
    volumes:
      - ops_tracker_pgdata:/var/lib/postgresql/data

volumes:
  ops_tracker_pgdata: